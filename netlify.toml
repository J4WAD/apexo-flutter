[build]
  publish = "build/web"
  command = """
    git clone https://github.com/flutter/flutter.git
    export PATH="$PATH:$PWD/flutter/bin"
    flutter precache
    flutter pub get
    flutter build web
  """