{
  "name": "apexo-flutter",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node install-dependencies.js",
    "build": "flutter build web",
    "deploy": "npm run build"
  },
  "dependencies": {
    "download": "^8.0.0",
    "extract-zip": "^2.0.1"
  }
}
