{
  "name": "apexo-flutter",
  "version": "1.0.0",
  "scripts": {
    "setup": "node build-setup.js",
    "build": "npm run setup && flutter build web --release",
    "deploy": "npm run build",
    "clean": "rm -rf build/"
  },
  "dependencies": {
    "download": "^8.0.0",
    "extract-zip": "^2.0.1"
  }
}
